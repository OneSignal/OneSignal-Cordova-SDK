import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ionic7-onesignal-example',
  webDir: 'dist',
};

export default config;
